# .ai-context.md

AIエージェントが作業開始時に現在の状態・進行中Issue・次の手を把握するためのコンテキストファイル。
**Project Instance では作業開始前に必ず読むこと。作業終了時に必ず更新すること。**

---

## 1. Status

works（Gemini API クライアント実装済み、PR #29 レビュー待ち）

---

## 2. Active Issues

- #18 Gemini API クライアント実装 → PR #29 レビュー待ち

---

## 3. Decisions

| 日付 | 決定事項 | 理由 | 関連Issue |
|---|---|---|---|
| 2026-03-21 | yo code を使わず手動スキャフォールド作成 | 依存を最小化し構成をコントロールするため | #2 |
| 2026-03-21 | extension-build を独立 CI ジョブとして追加 | 既存の os-self-test・ci ジョブを壊さずに Node.js ビルドを検証するため | #2 |
| 2026-03-21 | WebviewView 基盤をスキャフォールドと分離実装 | スキャフォールド(#2)は構造のみ、セッション連携ロジック(#15)を別 Issue に分割 | #15 |
| 2026-03-21 | TerminalOutputParser を VS Code 非依存の純粋 TS クラスとして実装 | 後続の統合・テストを容易にするため | #6 |
| 2026-03-21 | onDidWriteTerminalData（proposed API）を runtime guard 付きで使用 | VS Code 1.85 互換を保ちつつターミナル出力を取得するため | #6 |
| 2026-03-21 | Terminal Profile は contributes.terminal.profiles + registerTerminalProfileProvider で実装 | VS Code 1.73+ の正式 API を使用 | #3 |

---

## 4. Next actions

1. PR #29（Issue #18）をマージ後、`/pr-complete 29` で context 更新
2. 次は Issue #12（危険コマンド検出・警告表示）または Issue #19（日本語翻訳機能）に着手
3. 依存関係の優先順位: #2 ✅ → #15 ✅ → #6 ✅ → #3 ✅ → #4 ✅ → #8 ✅ → #9 ✅ → #18 ✅（PR中） → #12 or #19

---

## 5. Known pitfalls

- `package-lock.json` の diff が大きく diff size warning が出るが、初回 npm install のため想定内
- `terminal-sidecar-requirements.md` がリポジトリに存在しない（Issue #2 の参考文書）。後続 Issue 実装時に内容を確認すること
- `onDidWriteTerminalData` は proposed API。本番動作には VS Code を `--enable-proposed-api a2m-m.shirouto-code` で起動する必要がある。Issue #4 実装時に注意
- Issue #4（PTY）は `ExtensionTerminalOptions` を使う。Issue #3 の `managedTerminal` 追跡ロジックを流用できる ✅ 実装済み
- `managedTerminal` は Terminal Profile（Issue #3）と PTY（Issue #4）で共有。PTY 起動時に既存セッションがあれば再利用する設計（後続 Issue でセッション種別の識別が必要になる可能性あり）

---

## 6. Commands

（`project_config.yml` の `commands.*` に従う）
- `npm run compile` — TypeScript コンパイル
- `npx vsce package --no-dependencies` — VSIX 生成

---

## 7. CI Notes

| 典型原因 | 対処法 | 最終発生 |
|---|---|---|
