# .ai-context.md

AIエージェントが作業開始時に現在の状態・進行中Issue・次の手を把握するためのコンテキストファイル。
**作業開始前に必ず読むこと。作業終了時に必ず更新すること。**

---

## 1. Status

<!-- TODO: 現在の状態を works / broken で記載し、補足があれば追記する -->

| 項目 | 状態 |
|---|---|
| 全体 | `works` |
| 補足 | `Issue #10 実装完了・CIワークフロー整備完了` |

---

## 2. Active Issues

<!-- TODO: 現在進行中の Issue をリンク付きで記載する。完了済みは除外し、常に最新状態を保つ -->

| Issue | 要点 | 担当 |
|---|---|---|
| `[#15](https://github.com/a2m-m/AI-First-Development-Operating-System/issues/15)` | `コミット戦略の定義 — コミットメッセージ規約と巨大diff警告の仕組みを整備する` | `Antigravity` |

---

## 3. Decisions

<!-- TODO: 仕様・方針の意思決定を記録する。「なぜそうしたか」を必ず残す -->

| 日付 | 決定事項 | 理由 | 関連Issue |
|---|---|---|---|
| `2026-02-22` | `scripts/init をPythonで実装` | `sedのメタ文字による意図せぬ置換失敗・インジェクションを防ぎ堅牢にするため` | `#8` |
| `2026-02-22` | `Secret Scanをgitleaksで実装` | `trufflehogのGitHub Actionがcheckout@v4のPR挙動と相性が悪くsignal:killedで落ちるケースがあったため安定稼働するツールに切り替え` | `#10` |
| `2026-02-22` | `scripts/runのget_yaml_valueの修正` | `bash内のダブルクォートパースでエラー起因となる点を防ぐため、シングルクォート表記に変更` | `#10` |
| `2026-02-23` | `巨大diff警告をpre-push hookで実装する方針` | `開発プロセスの早い段階でAIや人間にフィードバックを返すため（warnで導入）` | `#15` |

---

## 4. Next actions

<!-- TODO: 優先度順に次のアクションを記載する。エージェントはここを見て次の作業を判断する -->

1. `Issue #15 の実装（ドキュメント作成・AIルールの更新）が完了。Claude Code による AI Review を依頼する`
2. `レビュー指摘事項があれば修正し、問題なければ main へマージする`

---

## 5. Known pitfalls

<!-- TODO: よくある罠・詰まりポイントを記載する。過去の失敗から学んだことをここに蓄積する -->

- `TODO: 罠1（例：○○の設定を忘れると CI が落ちる）`
- `TODO: 罠2`

---

## 6. Commands

<!-- TODO: プロジェクトで使用するコマンドを記載する。os-template.yml の commands.* と対応させる -->

| コマンド | 実行方法 | 備考 |
|---|---|---|
| format | `TODO: ./scripts/run format` | `TODO: 備考` |
| lint | `TODO: ./scripts/run lint` | `TODO: 備考` |
| typecheck | `TODO: ./scripts/run typecheck` | `TODO: 未使用の場合は理由を記載` |
| test | `TODO: ./scripts/run test` | `TODO: 備考` |
| build | `TODO: ./scripts/run build` | `TODO: 備考` |

---

## 7. CI Notes

<!-- TODO: CI が落ちた時の典型原因と対処法を記載する。繰り返し発生するものを優先的に記録する -->

| 典型原因 | 対処法 | 最終発生 |
|---|---|---|
| `GitHub Action (trufflehog) が signal: killed でサイレント終了する` | `trufflehog特有のPR diff生成時の不具合。Gitleaksなどへの代替・またはベースブランチの明示的なfetchの徹底。` | `2026-02-22` |
