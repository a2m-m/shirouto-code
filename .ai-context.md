# .ai-context.md

AIエージェントが作業開始時に現在の状態・進行中Issue・次の手を把握するためのコンテキストファイル。
**作業開始前に必ず読むこと。作業終了時に必ず更新すること。**

---

## 1. Status

<!-- TODO: 現在の状態を works / broken で記載し、補足があれば追記する -->

| 項目 | 状態 |
|---|---|
| 全体 | `works` |
| 補足 | `シングルエージェント移行完了。Issue #44 CIテスト統合の PR(#63) 作成済。` |

---

## 2. Active Issues

<!-- TODO: 現在進行中の Issue をリンク付きで記載する。完了済みは除外し、常に最新状態を保つ -->

| Issue | 要点 | 担当 |
| `[#38](https://github.com/a2m-m/AI-First-Development-Operating-System/issues/38)` | `Claude Codeによるプロジェクト推進状況のレビューと改善Issueの起票。完了。` | `Claude Code` |

---

## 3. Decisions

<!-- TODO: 仕様・方針の意思決定を記録する。「なぜそうしたか」を必ず残す -->

| 日付 | 決定事項 | 理由 | 関連Issue |
|---|---|---|---|
| `2026-02-23` | `Guardrail CIのAI初期導入はMock(実行はPython)とし、Severityは段階導入のためMEDIUMとした` | `AIモデル選定はスコープ外であり、まずは教育コメントのパース・警告フロー（warnモード）を確立するため` | `#17` |
| `2026-02-23` | `LCGのAI実装も一旦Mockとし、Pythonでパース・PRコメント・資産化(docs/learning/commits)のパイプラインを確立` | `LLM API連携自体は拡張可能にし、まずは「方針に基づく教育の資産化フロー」が動作する状態を満たすため` | `#18` |
| `2026-02-22` | `Secret Scanをgitleaksで実装` | `trufflehogのGitHub Actionがcheckout@v4のPR挙動と相性が悪くsignal:killedで落ちるケースがあったため安定稼働するツールに切り替え` | `#10` |
| `2026-02-22` | `scripts/runのget_yaml_valueの修正` | `bash内のダブルクォートパースでエラー起因となる点を防ぐため、シングルクォート表記に変更` | `#10` |
| `2026-02-23` | `巨大diff警告をpre-push hookで実装する方針` | `開発プロセスの早い段階でAIや人間にフィードバックを返すため（warnで導入）` | `#15` |
| `2026-02-23` | `scripts/hooks/pre-push のポリシー値抽出に awk を使用` | `シンプルに対応可能かつ不要な外部依存（yqなど）を防ぐため` | `#16` |
| `2026-02-23` | `Dependency ScanスキャンツールにTrivyを使用` | `軽量でGitHub Actions上でも即座に実行でき、CRITICAL/HIGHで即座にfailさせる設定が容易なため` | `#19` |
| `2026-02-23` | `CHANGELOG.mdの初期エントリ` | `Issue 1〜19の基盤整備の成果物を初期リリース「0.1.0」として列挙した` | `#20` |
| `2026-02-23` | `docs/tuning-guide.mdの作成とREADMEからのリンク追加` | `「人を注意するのではなく仕組みを更新する」OSの改善ループプロセスを実運用に落とし込むため` | `#21` |
| `2026-02-23` | `Claude.jsonの作成とCLAUDE.mdの更新` | `Claude Codeが常にSonnet等の最新モデルを使用するよう挙動を制御するため` | `#36` |
| `2026-02-25` | `フォルダ構成整理：claude-code-mcp-migration-guide.md を docs/ へ移動、一時ファイル pr_body.md を削除` | `ルートディレクトリの一時ファイル削除と正しい配置への修正のため` | `#37` |
| `2026-02-25` | `kaizen.mdとClaude Codeの提言を元に13の改善Issueを新規起票し、OS原則の徹底とMock脱却を図るアプローチを決定` | `OSのもつ価値（教育、ガードレール）を実際に機能させるため` | `#38` |
| `2026-02-25` | `Claude Code レビューの MCP ワンショット方式への移行完了` | `Antigravity側のコンテキスト消費削減と、構造化出力によるパース容易性向上のため` | `#40` |
| `2026-02-25` | `scripts/guardrail のモックをGemini APIによる実通信に置き換え` | `OSの中核価値（教育とガードレール）を実際に機能させるため。ユーザー指示によりGeminiを利用` | `#42` |
| `2026-02-28` | `LCG生成ファイル名をAI生成のTopicベースに動的化 (Issue #43)` | `同一PR下での資産ファイル名衝突を防ぐため末尾にハッシュを付与し、さらに内容が直感的にわかるようにするため` | `#43` |
| `2026-02-28` | `os-template.ymlでunittestを有効化しCI品質ゲートを実装 (Issue #44)` | `OSテンプレート自体への品質担保の仕組みを連携・稼働させるため` | `#44` |
| `2026-02-28` | `scripts/run の YAML パースを config.py で共通化・堅牢化 (Issue #45)` | `PyYAMLを使ってバグを防ぐとともに、未インストール環境におけるサイレント障害を防ぐGraceful fallbackを実装した` | `#45` |
| `2026-02-28` | `Claude Code Hooks による機密ファイルアクセス等のセキュリティ強制の追加 (Issue #47)` | `AIが意図せず秘匿情報（.env等）を読み書きするリスクを軽減するため` | `#47` |
| `2026-03-16` | `シングルエージェント移行：Antigravity を廃止し Claude Code が Executor + QA/Architect を兼務` | `2エージェント構成の複雑性解消・MCP依存（#40/#58）の陳腐化。Issue #40（MCP移行）と #58（MCPハングアップ）はこの移行により不要となりクローズ。` | `-` |

---

## 4. Next actions

<!-- TODO: 優先度順に次のアクションを記載する。エージェントはここを見て次の作業を判断する -->

1. `Issue #40（MCP移行）と Issue #58（MCPハングアップ）をクローズする（Antigravity廃止により陳腐化）`
2. `Issue #38 に基づき、Claude Codeを用いたプロジェクト推進状況レビューとさらなるIssue起票を行う`
3. `シングルエージェント移行後の動作確認：Claude Code に実装タスクを与えたとき自己レビュー（/review）を自発的に実行するか確認`

---

## 5. Known pitfalls

<!-- 過去の失敗・詰まりポイントを蓄積する。新たな罠を発見したらここに追記する -->

- `GitHub Action (trufflehog) が signal: killed でサイレント終了する不具合があるため、Gitleaks等を利用するかベースブランチの明示的なfetchが必要`
- `pre-push hook はホスト環境(macOS/Linuxなど)によってawkやgrepの挙動が変わる可能性があるため、依存を減らしたシンプルなスクリプト記述を心がける`

---

## 6. Commands

<!-- このリポジトリは OS Template Repo のため、各開発プロジェクトにおける固有コマンドは、ここから派生した Instance 側のリポジトリで設定・運用されます。 -->
<!-- 当リポジトリ（OS自体）のCI/CDやローカル検証用としては、以下のコマンドを利用します。 -->

| コマンド | 実行方法 | 備考 |
|---|---|---|
| doctor | `./scripts/run doctor` | `環境依存関係（python, bash, gitなど）のチェック` |
| ci | `./scripts/run ci` | `PR作成時等に実行される各種チェック・テストのフローマスター` |
| format | `./scripts/run format` | `コードフォーマットの実行` |
| lint | `./scripts/run lint` | `静的解析の実行` |
| typecheck | `./scripts/run typecheck` | `将来の拡張用・現在は未実装` |
| test | `./scripts/run test` | `単体テスト（Python unittest）の実行` |
| build | `./scripts/run build` | `将来の拡張用・現在は未実装` |

---

## 7. CI Notes

<!-- TODO: CI が落ちた時の典型原因と対処法を記載する。繰り返し発生するものを優先直に記録する -->

| 典型原因 | 対処法 | 最終発生 |
|---|---|---|
| `GitHub Action (trufflehog) が signal: killed でサイレント終了する` | `trufflehog特有のPR diff生成時の不具合。Gitleaksなどへの代替・またはベースブランチの明示的なfetchの徹底。` | `2026-02-22` |
